apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tb-node-0
  name: tb-node-0
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tb-node-0
  strategy: {}
  template:
    metadata:
      labels:
        app: tb-node-0
    spec:
      serviceAccountName: default
      volumes:
      - name: vol
        emptyDir: {}
      containers:
      - image: replace
        name: tb-node-0-container
        volumeMounts:
          - mountPath: /tmp
            name: vol
        securityContext:
          capabilities:
            drop:
             - NET_RAW
          runAsUser: 100
          runAsNonRoot: true
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        resources:
         requests:
          memory: "256Mi"
          cpu: "200m"
         limits:
          memory: "512Mi"
          cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: tb-node-0
  name: tb-node-0-svc
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: tb-node-0
  type: ClusterIP